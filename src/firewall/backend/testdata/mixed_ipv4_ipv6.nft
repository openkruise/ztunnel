add table inet ztunnel
delete table inet ztunnel
table inet ztunnel {
  chain zt_policy_input {
    type filter hook input priority -150; policy accept;
    ct state established,related accept
    iif lo accept
    ip saddr 10.0.0.0/8 ip daddr 192.168.1.0/24 meta l4proto { udp, sctp } th dport 443 reject comment "mixed-family"
    ip6 saddr fd00::/8 ip6 daddr 2001:db8::/32 meta l4proto { udp, sctp } th dport 443 reject comment "mixed-family"
  }
  chain zt_policy_output {
    type filter hook output priority -150; policy accept;
    ct state established,related accept
    oif lo accept
    meta skuid 1337 return
    meta skgid 1337 return
  }
}
